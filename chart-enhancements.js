(function(){
  const style=document.createElement('style');style.textContent='.chart-y-axis-left,.chart-y-axis-right{position:absolute;top:0;bottom:0;width:58px;pointer-events:none;z-index:4;font:700 10px Inter,system-ui,sans-serif}.chart-y-axis-left{left:0}.chart-y-axis-right{right:0;text-align:left}.chart-y-axis-left span,.chart-y-axis-right span{position:absolute;transform:translateY(-50%);white-space:nowrap}.chart-y-axis-left span{right:8px;color:#7189a2}.chart-y-axis-right span{left:8px}.chart-y-axis-right .up{color:#55dfab}.chart-y-axis-right .down{color:#ff9c88}.chart-stage{touch-action:pan-y;contain:layout paint}.chart-stage svg{display:block;will-change:transform;backface-visibility:hidden;transform:translateZ(0)}.chart-stage [data-plot-group]{will-change:transform;transform-box:fill-box;transform-origin:0 50%;backface-visibility:hidden}.chart-candles{opacity:1}.chart-candle-wick,.chart-candle-cap{display:none}.chart-candle-body{stroke-width:2;vector-effect:non-scaling-stroke}.chart-line{display:none}@media(max-width:650px){.chart-y-axis-left,.chart-y-axis-right{width:45px}.chart-y-axis-left span{right:5px}.chart-y-axis-right span{left:5px}}';document.head.appendChild(style);
  const chartState={grid:true,crosshair:false,dots:true,zoom:1};
  const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
  const money=v=>'Rp'+Math.round(Number(v)||0).toLocaleString('id-ID');
  const dateLabel=d=>new Intl.DateTimeFormat('id-ID',{day:'numeric',month:'short'}).format(new Date(`${d}T00:00:00`)).replace('.','');
  const pct=(now,prev)=>prev===0?(now===0?0:null):((now-prev)/Math.abs(prev))*100;
  const pctText=v=>v===null?'—':`${v>=0?'+':''}${v.toFixed(1)}%`;
  const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));

  window.renderTrendChart=function(){
    const el=document.getElementById('trendChart');if(!el)return;
    const reports=Object.values(getReports()).filter(r=>r&&r.date).sort((a,b)=>a.date.localeCompare(b.date));
    const hasData=reports.length>0;
    const data=hasData?reports:Array.from({length:4},(_,i)=>({date:`2000-01-${String(i+1).padStart(2,'0')}`,income:0,profit:0}));
    const w=1000,h=390,pad={l:78,r:78,t:28,b:68},innerW=w-pad.l-pad.r,innerH=h-pad.t-pad.b;
    const baseIncome=hasData?(Number(data[0].income)||0):0,baseProfit=hasData?(Number(data[0].profit)||0):0;
    const performance=r=>baseIncome?((Number(r.income)||0)/baseIncome)*100:0;
    const profitPerf=r=>baseProfit?((Number(r.profit)||0)/baseProfit)*100:0;
    const perf=data.map(performance),maxPerf=hasData?Math.max(100,...perf):100,maxY=Math.ceil((maxPerf+10)/10)*10,range=Math.max(100,maxY),y=v=>pad.t+(maxY-v)*innerH/range,zeroY=y(0),baseStep=data.length>1?Math.max(62,innerW/Math.min(data.length,9)):72,x=i=>pad.l+20+i*baseStep*chartState.zoom,rightX=w-pad.r;
    const grid=Array.from({length:5},(_,i)=>{const v=maxY-range*(i/4);return `<line x1="${pad.l}" y1="${y(v)}" x2="${rightX}" y2="${y(v)}" class="chart-grid-line"/>`}).join('');
    const leftTicks=Array.from({length:5},(_,i)=>{const v=maxY-range*(i/4);return `<span style="top:${(y(v)/h)*100}%">${Math.round(v)}%</span>`}).join('');
    const changeLabels=hasData?data.map((r,i)=>{const ch=i===0?0:pct(Number(r.income)||0,Number(data[i-1].income)||0);return `<span data-change-label="${i}" class="${ch>=0?'up':'down'}" style="top:${(y(performance(r))/h)*100}%">${i===0?'0%':pctText(ch)}</span>`}).join(''):'';
    const labels=data.map((r,i)=>hasData?`<text data-date-label="${i}" x="${x(i)}" y="${h-20}" text-anchor="middle" class="chart-label">${dateLabel(r.date)}</text>`:'').join('');
    const candles=hasData?data.map((r,i)=>{const cur=Number(r.income)||0,prev=i>0?(Number(data[i-1].income)||0):cur,ch=i===0?0:pct(cur,prev)||0,value=Math.max(0,performance(r)),top=Math.max(pad.t,y(value)),height=Math.max(8,zeroY-top),bodyW=30;return `<g data-candle="${i}" class="chart-candle-group ${ch>=0?'up':'down'}"><rect x="${x(i)-bodyW/2}" y="${top}" width="${bodyW}" height="${height}" rx="5" class="chart-candle-body"/></g>`}).join(''):'';
    const dots=(cls,fn)=>data.map((r,i)=>`<circle data-dot="${i}" cx="${x(i)}" cy="${y(fn(r))}" r="5" class="chart-data-dot ${cls}"/>`).join('');
    const last=hasData?data[data.length-1]:null,prev=hasData&&data.length>1?data[data.length-2]:null,lastChange=prev?pct(Number(last.income)||0,Number(prev.income)||0):0;
    el.innerHTML=`<div class="chart-stage" data-chart-stage><div class="chart-y-axis-left">${leftTicks}</div><div class="chart-y-axis-right">${changeLabels}</div><svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none" role="img" aria-label="Grafik performa dan perubahan omzet"><defs><clipPath id="chartClip"><rect x="${pad.l}" y="${pad.t}" width="${innerW}" height="${innerH}"/></clipPath></defs><g class="chart-fixed-grid"><g class="chart-grid-layer ${chartState.grid?'visible':''}">${grid}</g></g><line x1="${pad.l}" y1="${pad.t}" x2="${pad.l}" y2="${h-pad.b}" class="chart-axis-line"/><line x1="${rightX}" y1="${pad.t}" x2="${rightX}" y2="${h-pad.b}" class="chart-axis-line"/><g data-plot-group clip-path="url(#chartClip)"><g class="chart-candles">${candles}</g><g class="chart-dots-layer ${chartState.dots?'visible':''}">${dots('chart-income-dot',performance)}${dots('chart-profit-dot',profitPerf)}</g></g><g class="chart-date-labels">${labels}</g><line x1="${pad.l}" y1="${zeroY}" x2="${rightX}" y2="${zeroY}" class="chart-axis-line"/><g class="chart-crosshair-layer ${chartState.crosshair?'visible':''}"><line data-crosshair-x x1="${pad.l}" y1="${pad.t}" x2="${pad.l}" y2="${h-pad.b}" class="chart-crosshair-x"/><line data-crosshair-y x1="${pad.l}" y1="${y(hasData?performance(last):0)}" x2="${rightX}" y2="${y(hasData?performance(last):0)}" class="chart-crosshair-y"/><circle data-crosshair-dot cx="${pad.l}" cy="${y(hasData?performance(last):0)}" r="7" class="chart-crosshair-dot"/></g></svg><div class="chart-tooltip" data-chart-tooltip hidden></div></div><div class="chart-bottom"><div class="chart-legend"><span><i class="legend-income"></i> Omzet</span><span><i class="legend-profit"></i> Keuntungan</span>${hasData&&prev?`<span class="chart-change ${lastChange>=0?'up':'down'}">${pctText(lastChange)}</span>`:'<span class="chart-last-readout">Tambahkan laporan harian untuk melihat perubahan</span>'}</div><div class="chart-tools"><button type="button" class="chart-toggle ${chartState.grid?'on':''}" data-chart-toggle="grid">▦ Grid</button><button type="button" class="chart-toggle ${chartState.crosshair?'on':''}" data-chart-toggle="crosshair">＋ Garis</button><button type="button" class="chart-toggle ${chartState.dots?'on':''}" data-chart-toggle="dots">● Titik</button></div></div>`;
    el.querySelectorAll('[data-chart-toggle]').forEach(btn=>btn.onclick=()=>{chartState[btn.dataset.chartToggle]=!chartState[btn.dataset.chartToggle];renderTrendChart();});
    bindInteractions(el,data,w,h,pad,x,y,performance);
  };

  function applyPlotZoom(el,pad,zoom,x){const group=el.querySelector('[data-plot-group]');if(group)group.style.transform=`translate3d(${pad.l}px,0,0) scale3d(${zoom},1,1) translate3d(${-pad.l}px,0,0)`;el.querySelectorAll('[data-date-label]').forEach((label,i)=>label.setAttribute('x',x(i)));}

  function bindInteractions(el,data,w,h,pad,x,y,performance){
    const stage=el.querySelector('[data-chart-stage]'),svg=stage.querySelector('svg'),tip=stage.querySelector('[data-chart-tooltip]');let pinchStart=null,lastZoom=chartState.zoom,targetZoom=chartState.zoom,raf=0;
    const renderFrame=()=>{raf=0;const delta=targetZoom-chartState.zoom;if(Math.abs(delta)<.001){chartState.zoom=targetZoom;applyPlotZoom(el,pad,chartState.zoom,x);return;}chartState.zoom+=(delta)*.32;applyPlotZoom(el,pad,chartState.zoom,x);raf=requestAnimationFrame(renderFrame);};
    const scheduleZoom=z=>{targetZoom=clamp(z,.65,4);if(!raf)raf=requestAnimationFrame(renderFrame);};
    const move=e=>{if(!chartState.crosshair||!data.length)return;const rect=svg.getBoundingClientRect();let px=(e.clientX-rect.left)/rect.width*w;px=clamp(px,pad.l,w-pad.r);const step=Math.max(62,(w-pad.l-pad.r)/Math.min(data.length,9)),i=Math.max(0,Math.min(data.length-1,Math.round((px-pad.l-20)/(step*chartState.zoom)))),sx=x(i),income=Number(data[i].income)||0,profit=Number(data[i].profit)||0,prev=i>0?(Number(data[i-1].income)||0):0,ch=i>0?pct(income,prev):0,cx=stage.querySelector('[data-crosshair-x]'),cy=stage.querySelector('[data-crosshair-y]'),dot=stage.querySelector('[data-crosshair-dot]');cx.setAttribute('x1',sx);cx.setAttribute('x2',sx);cy.setAttribute('y1',y(performance(data[i])));cy.setAttribute('y2',y(performance(data[i])));dot.setAttribute('cx',sx);dot.setAttribute('cy',y(performance(data[i])));tip.hidden=false;tip.innerHTML=`<b>${esc(dateLabel(data[i].date))}</b><span>Omzet ${money(income)}</span><span>Profit ${money(profit)}</span><strong class="tip-change ${ch>=0?'up':'down'}">${pctText(ch)}</strong>`;tip.style.left=`${clamp((sx/w)*100,5,82)}%`;};
    stage.addEventListener('pointermove',move,{passive:true});stage.addEventListener('pointerleave',()=>tip.hidden=true,{passive:true});
    stage.addEventListener('touchstart',e=>{if(e.touches.length===2){pinchStart=Math.hypot(e.touches[0].clientX-e.touches[1].clientX,e.touches[0].clientY-e.touches[1].clientY);lastZoom=targetZoom;}},{passive:true});
    stage.addEventListener('touchmove',e=>{if(e.touches.length===2&&pinchStart){const dist=Math.hypot(e.touches[0].clientX-e.touches[1].clientX,e.touches[0].clientY-e.touches[1].clientY),next=lastZoom*(dist/pinchStart);scheduleZoom(next);e.preventDefault();}},{passive:false});
    stage.addEventListener('touchend',()=>{pinchStart=null;lastZoom=targetZoom;},{passive:true});
  }
  renderTrendChart();
})();
